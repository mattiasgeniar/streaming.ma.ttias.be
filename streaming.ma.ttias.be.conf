streaming.ma.ttias.be {
    tls internal

    reverse_proxy localhost:8000

    encode zstd gzip

    log {
        output file /var/www/html/streaming.ma.ttias.be/logs/access.log
    }

    header {
        Strict-Transport-Security "max-age=31536000"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "no-referrer"
        Permissions-Policy "geolocation=(), microphone=(), camera=()"
        -Server
    }

    # Cache API responses briefly (titles change at most once per day)
    @api path /api/*
    header @api Cache-Control "public, max-age=300"

    # Cache static assets (poster images are proxied from JustWatch via browser)
    @static path /static/*
    header @static Cache-Control "public, max-age=86400"
}
